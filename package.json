{
  "name": "auction-tracker-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Electron desktop wrapper for Auction Tracker",
  "main": "electron/main.cjs",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:electron": "cross-env ELECTRON_USE_EXTERNAL_BACKEND=1 wait-on tcp:3001 http://127.0.0.1:5173 && electron .",
    "dev": "concurrently -k -n BACKEND,FRONTEND,ELECTRON -c yellow,cyan,green \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:electron\"",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build:frontend:electron": "cross-env ELECTRON_BUILD=true VITE_API_URL=http://127.0.0.1:3001/api/v1 VITE_WS_URL=http://127.0.0.1:3001 npm --prefix frontend run build",
    "build:electron": "npm run build:backend && npm run build:frontend:electron",
    "dist": "npm run build:electron && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder",
    "dist:mac": "npm run build:electron && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "^37.4.0",
    "electron-builder": "^26.0.12",
    "wait-on": "^9.0.1"
  },
  "build": {
    "appId": "com.auctiontracker.desktop",
    "productName": "AuctionTracker",
    "directories": {
      "output": "release"
    },
    "files": [
      "electron/**/*",
      "frontend/dist/**/*"
    ],
    "extraResources": [
      {
        "from": "backend/dist",
        "to": "backend/dist"
      },
      {
        "from": "backend/node_modules",
        "to": "backend/node_modules"
      },
      {
        "from": "backend/package.json",
        "to": "backend/package.json"
      }
    ],
    "mac": {
      "identity": null,
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.business"
    }
  }
}
