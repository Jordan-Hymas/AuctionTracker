# Auction Tracker Program

# DUE Feb 26th

Speed up the paddle animation so that it doesnt take so long to display the paddle number

[X] Paddle shifting animation 3s 
[X] Change control panel to default to dark theme unless changed
[] Control panel optimize layout and clean up the UI
[X] Change Vertical thermometer should look "like rectangle rounded edges" add option to choose
[X] When goal reached on main screen it should have confettie with the thermometer moving. 
[X] Four paddle digits should not allow 3 digit paddle numbers 
[X] Goal reached screen should allow you to add an image to the background just like the custom theme does
[X] On the control panel setup section add a way to change the progress bar (Thermometer, pill, ex...)
[X] Paddle number display should have an animation like a presentation 3s delay for animation 
[X] Start screen before paddle numbers are entered needs to be fixed 
[X] Paddle Numbers need to be at least 4 ints instead of 3
[X] Control Panel: Change name of displays "Normal" to "Fundraiser page"
[X] Check mobile view, should allow you to enter bids without keyboard closing or have an enter button on the keyboard # only.
[X] Fix the timestamps on the control panel recent bids section and also make sure the csv download shows it correctly.

[] Use Electronjs.org to make an exucatable for windows. https://www.electronjs.org/
[] Works on Windows machine 
[] Package the project and deploy on different pc's
[] Resolution to fit any display 1920-1080p
[] Clean the directory make sure everything looks great, package, deploy, and testing.
[] Make sure everything stays centered on any display size


# Final steps

[Claude] Clean the project remove all the junk not being used! Docker, images, loose code. 
[Claude] Check for any latency concerns on slow networks and optimze. 
[Claude] add electronjs.org to the project, compile and test. Find a bugs or issues with this. 
[Me] Package with defualt logos, NPCE, BGCA, ex..
[Me] Package with custom backgrounds for main display. 
[Chat] add this to projects page of portfolio

	

